# To run, use the command:
## docker run -d --env-file .env route07/binance-futures-bot:1.9.2 ##
# Distributed Hedging API Configuration (Anti-Detection)
# Primary orders key
BINANCE_API_KEY=
BINANCE_SECRET_KEY=
BINANCE_TESTNET=false
# Secondary API key for hedge positions to reduce detection patterns
# HEDGE_API_KEY=
# HEDGE_SECRET_KEY=
USE_DISTRIBUTED_HEDGING=false

# Multi-Pair Trading Bot Configuration
# This configuration allows running multiple trading pairs independently

# =============================================================================
# GLOBAL CONFIGURATION
# =============================================================================

# Active Trading Pairs (comma-separated)
# Each pair will run in its own instance
ACTIVE_PAIRS=ADAUSDT,ETHUSDT,BNBUSDT
ETH_ENABLED=true
ADA_ENABLED=true
BTC_ENABLED=false
BNB_ENABLED=true
# System Configuration
PRICE_UPDATE_INTERVAL=20
HISTORICAL_4H_DAYS=180
HISTORICAL_1H_DAYS=7
HISTORICAL_15M_DAYS=1

# Global Technical Analysis (applies to all pairs unless overridden)
RSI_PERIOD=14
EMA_FAST=9
EMA_SLOW=18
VOLUME_PERIOD=20
VOLUME_MULTIPLIER=0.6

# Global Take Profit Percentages (applies to all pairs unless overridden)
ANCHOR_TP_PERCENT=1.0
OPPORTUNITY_TP_PERCENT=1.0
SCALP_TP_PERCENT=0.5

# Global Dynamic Levels (applies to all pairs)
USE_DYNAMIC_LEVELS=true
LEARNING_PERIOD=180
FOUR_HOUR_WEIGHT=1.0
ONE_HOUR_WEIGHT=0.7
FIFTEEN_MIN_WEIGHT=0.4

# Global Position Sizing (applies to all pairs unless overridden)
BASE_ANCHOR_SIZE=0.20
BASE_HEDGE_SIZE=0.28
BASE_OPPORTUNITY_SIZE=0.20
BASE_SCALP_SIZE=0.20
BASE_SCALP_HEDGE_SIZE=0.25
MAX_POSITION_SIZE=0.25

# Global Leverage Settings (applies to all pairs unless overridden)
ANCHOR_LEVERAGE=20
HEDGE_LEVERAGE=25
OPPORTUNITY_LEVERAGE=20
SCALP_LEVERAGE=20
SCALP_HEDGE_LEVERAGE=25

# Emergency Hedge Leverage (for failed hedge recovery)
EMERGENCY_HEDGE_LEVERAGE=20

# Logging
LOG_LEVEL=info
LOG_FILE=logs/trading-bot.log

# =============================================================================
# AI INTEGRATION CONFIGURATION
# =============================================================================

# DeepSeek API Configuration
DEEPSEEK_API_KEY=your_deepseek_api_key_here
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# AI Feature Toggles
AI_ENABLE_SENTIMENT=true
AI_ENABLE_PATTERNS=true
AI_ENABLE_REGIME=true
AI_ENABLE_RISK=true
AI_ENABLE_CORRELATION=true

# AI Analysis Intervals (in minutes)
AI_SENTIMENT_INTERVAL=15
AI_PATTERN_INTERVAL=10
AI_REGIME_INTERVAL=30
AI_RISK_INTERVAL=20

# API Rate Limiting
AI_MAX_API_CALLS=50

# Fallback Behavior
AI_FALLBACK_TO_TECHNICAL=true

# =============================================================================
# ADA/USDT CONFIGURATION
# =============================================================================

# ADA Basic Settings
ADA_BASE_BALANCE=1000
# ADA_ENABLED=true # Already configured above

# ADA Position Sizing (optional - inherits from global if not specified)
# ADA_ANCHOR_POSITION_SIZE=0.20    # Uses global BASE_ANCHOR_SIZE=0.20
# ADA_ANCHOR_HEDGE_SIZE=0.30       # Uses global BASE_HEDGE_SIZE=0.30
# ADA_OPPORTUNITY_POSITION_SIZE=0.20  # Uses global BASE_OPPORTUNITY_SIZE=0.20
# ADA_OPPORTUNITY_HEDGE_SIZE=0.30  # Uses global BASE_HEDGE_SIZE=0.30
# ADA_SCALP_POSITION_SIZE=0.10     # Uses global BASE_SCALP_SIZE=0.10
# ADA_SCALP_HEDGE_SIZE=0.10        # Uses global BASE_SCALP_HEDGE_SIZE=0.10

# ADA Leverage Settings (optional - inherits from global if not specified)
# ADA_ANCHOR_LEVERAGE=10    # Uses global ANCHOR_LEVERAGE=10
# ADA_HEDGE_LEVERAGE=15     # Uses global HEDGE_LEVERAGE=15
# ADA_OPPORTUNITY_LEVERAGE=10  # Uses global OPPORTUNITY_LEVERAGE=10
# ADA_SCALP_LEVERAGE=15     # Uses global SCALP_LEVERAGE=15
# ADA_SCALP_HEDGE_LEVERAGE=15  # Uses global SCALP_HEDGE_LEVERAGE=15
# ADA_EMERGENCY_HEDGE_LEVERAGE=25  # Uses global EMERGENCY_HEDGE_LEVERAGE=20

# ADA Technical Analysis (optional - inherits from global if not specified)
# ADA_RSI_PERIOD=14           # Uses global RSI_PERIOD=14
# ADA_EMA_FAST=9              # Uses global EMA_FAST=9
# ADA_EMA_SLOW=18             # Uses global EMA_SLOW=18
# ADA_VOLUME_PERIOD=20        # Uses global VOLUME_PERIOD=20
# ADA_VOLUME_MULTIPLIER=1.2   # Uses global VOLUME_MULTIPLIER=1.2

# ADA Support/Resistance Levels
ADA_RESISTANCE_1=0.8673
ADA_RESISTANCE_2=0.8816
ADA_RESISTANCE_3=0.8837
ADA_SUPPORT_1=0.8602
ADA_SUPPORT_2=0.8598
ADA_SUPPORT_3=0.8389

# ADA Take Profit Percentages (optional - inherits from global if not specified)
# ADA_ANCHOR_TP_PERCENT=2.0      # Uses global ANCHOR_TP_PERCENT=2.0
# ADA_OPPORTUNITY_TP_PERCENT=1.5 # Uses global OPPORTUNITY_TP_PERCENT=1.5
# ADA_SCALP_TP_PERCENT=0.5       # Uses global SCALP_TP_PERCENT=0.5

# ADA Dynamic Levels (optional - inherits from global if not specified)
# ADA_USE_DYNAMIC_LEVELS=true    # Uses global USE_DYNAMIC_LEVELS=true
# ADA_LEARNING_PERIOD=180        # Uses global LEARNING_PERIOD=180
# ADA_4H_WEIGHT=1.0              # Uses global FOUR_HOUR_WEIGHT=1.0
# ADA_1H_WEIGHT=0.7              # Uses global ONE_HOUR_WEIGHT=0.7
# ADA_15M_WEIGHT=0.4             # Uses global FIFTEEN_MIN_WEIGHT=0.4

# =============================================================================
# ETH/USDT CONFIGURATION
# =============================================================================

# ETH Basic Settings
ETH_BASE_BALANCE=1000
#ETH_ENABLED=true # Already configured above

# ETH Position Sizing (optional - inherits from global if not specified)
# ETH_ANCHOR_POSITION_SIZE=0.20    # Uses global BASE_ANCHOR_SIZE=0.20
# ETH_ANCHOR_HEDGE_SIZE=0.30       # Uses global BASE_HEDGE_SIZE=0.30
# ETH_OPPORTUNITY_POSITION_SIZE=0.20  # Uses global BASE_OPPORTUNITY_SIZE=0.20
# ETH_OPPORTUNITY_HEDGE_SIZE=0.30  # Uses global BASE_HEDGE_SIZE=0.30
# ETH_SCALP_POSITION_SIZE=0.10     # Uses global BASE_SCALP_SIZE=0.10
# ETH_SCALP_HEDGE_SIZE=0.10        # Uses global BASE_SCALP_HEDGE_SIZE=0.10

# ETH Leverage Settings (optional - inherits from global if not specified)
# ETH_ANCHOR_LEVERAGE=10    # Uses global ANCHOR_LEVERAGE=10
# ETH_HEDGE_LEVERAGE=15     # Uses global HEDGE_LEVERAGE=15
# ETH_OPPORTUNITY_LEVERAGE=10  # Uses global OPPORTUNITY_LEVERAGE=10
# ETH_SCALP_LEVERAGE=15     # Uses global SCALP_LEVERAGE=15
# ETH_SCALP_HEDGE_LEVERAGE=15  # Uses global SCALP_HEDGE_LEVERAGE=15
# ETH_EMERGENCY_HEDGE_LEVERAGE=25  # Uses global EMERGENCY_HEDGE_LEVERAGE=20

# ETH Technical Analysis
ETH_RSI_PERIOD=14
ETH_EMA_FAST=9
ETH_EMA_SLOW=18
ETH_VOLUME_PERIOD=20
ETH_VOLUME_MULTIPLIER=1.2

# ETH Support/Resistance Levels (from ETH cheatsheet)
ETH_RESISTANCE_1=6287.523
ETH_RESISTANCE_2=5282.471
ETH_RESISTANCE_3=5142.954
ETH_SUPPORT_1=4953.929
ETH_SUPPORT_2=4809.849
ETH_SUPPORT_3=4737.810

# ETH Take Profit Percentages
ETH_ANCHOR_TP_PERCENT=2.0
ETH_OPPORTUNITY_TP_PERCENT=1.5
ETH_SCALP_TP_PERCENT=0.5

# ETH Dynamic Levels (optional - inherits from global USE_DYNAMIC_LEVELS)
# ETH_USE_DYNAMIC_LEVELS=true  # Override global setting if needed
ETH_LEARNING_PERIOD=180
ETH_4H_WEIGHT=1.0
ETH_1H_WEIGHT=0.7
ETH_15M_WEIGHT=0.4

# =============================================================================
# USAGE EXAMPLES
# =============================================================================

# Single Pair (ADA only):
# ACTIVE_PAIRS=ADAUSDT

# Multiple Pairs:
# ACTIVE_PAIRS=ADAUSDT,ETHUSDT

# Different API Keys per Pair (future feature):
# ADA_API_KEY=ada_api_key
# ETH_API_KEY=eth_api_key

# Different Balances per Pair:
# ADA_BASE_BALANCE=1000
# ETH_BASE_BALANCE=2000

# Different Strategies per Pair:
# ADA_ANCHOR_LEVERAGE=10
# ETH_ANCHOR_LEVERAGE=15
